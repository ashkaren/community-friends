
<div class="info">
  <h4 class="name"><%= meeting.name %></h4>
  <span class="action">created an <%= link_to 'event', meeting %></span>
</div>
<div class="content">
  <h3><%= meeting.name %></h3>
  <span class="text">
    <%= fa_icon'calendar' %> <%= formatted_time(meeting.when) %>
  </span>
  <%= render 'appwide/created_at', activity: meeting %>
  <div class="points">
    <span class="like-count"><%= pluralize(meeting.cached_votes_up, 'like') %></span>
    <span class="comment-count"><%= pluralize(meeting.comments_count, 'comment') %></span>
    <div class="pull-right">
      <% if belongs_to_user?(event) %>
        <%= form_tag meeting_path(meeting), method: :delete, remote: remote do %>
          <% button_tag class: 'btn btn-danger btn-sm' do %>
            <%= fa_icon 'trash' %>
          <% end %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
<%= render 'appwide/actions', resource: meeting %>
